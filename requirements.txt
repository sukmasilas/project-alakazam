# Milestone 2 — core data model + business logic (no web UI, no external
# integrations yet). Deliberately minimal, mirrors the same slice of
# Project-Noctrowl's own milestone-2 dependency set (sibling project, same
# tech stack: Python + PostgreSQL).
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12
python-dotenv==1.2.3
pytest==9.1.1

# Milestone 3 — real web app (backend + UI) against real Postgres data.
# FastAPI chosen over Flask/Django (see docs/design/milestone-3-web-app-design.md
# for the full reasoning): heavy client-side interactivity backed by a typed
# JSON API (Pydantic request/response validation) fits this project's UI
# better than Noctrowl's own mostly-server-rendered Flask app.
fastapi==0.141.1
uvicorn[standard]==0.52.4
jinja2==3.1.6
itsdangerous==2.2.0
python-multipart==0.0.32
httpx==0.28.1

# Milestone 4 — the Noctrowl export pipeline (Google Drive upload). Same
# package choices and version pins as Project-Noctrowl's own ingestion
# layer (sibling project) for consistency, though Alakazam's own OAuth
# client/token are entirely separate credentials (see .env.example and
# scripts/authorize_google_drive.py) — this project never reuses Noctrowl's
# Drive access. Only OAuth (user-consent) auth is implemented here — no
# service-account fallback, since Alakazam is a fresh build with no legacy
# service-account history to carry forward (unlike Noctrowl's).
google-api-python-client==2.199.0
google-auth==2.57.0
google-auth-httplib2==0.4.2
google-auth-oauthlib==1.4.1
